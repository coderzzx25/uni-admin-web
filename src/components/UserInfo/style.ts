import { Dropdown } from 'antd';
import styled from 'styled-components';

const UserInfoWrapper = styled(Dropdown)`
  display: flex;
  align-items: center;

  span {
    margin-left: 0.625rem;
  }
`;

export default UserInfoWrapper;
